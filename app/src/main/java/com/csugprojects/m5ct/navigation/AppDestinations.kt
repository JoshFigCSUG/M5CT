package com.csugprojects.m5ct.navigation

/**
 * Defines all constant string routes used for Compose navigation (NavHost).
 * Centralizing these constants ensures type safety and consistency across the application.
 */
object Routes {
    // Route for the main photo grid screen (Gallery).
    const val GALLERY = "gallery_screen"

    // Route for the CameraX capture screen (M5 Multimedia requirement).
    const val CAMERA = "camera_screen"

    // Route for the single photo detail view.
    const val DETAIL = "detail_screen"
}